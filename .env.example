# Database
DATABASE_URL=postgresql://user:password@host:port/database

# JWT
JWT_SECRET=clé_secrète

# Email
SMTP_HOST=smtp_host
SMTP_PORT=smtp_port
SMTP_USER=smtp_user
SMTP_PASSWORD=smtp_password
SMTP_FROM=smtp_from

# API
API_URL=api_url

# CORS
NODE_ENV=production
ALLOWED_ORIGINS=url_de_deploiement